import { createContext, useContext, useState } from "react";
import { category, infoPage, orders, products, sections, stores } from '../../index.js'

export default function dataContext() {
  return <div>dataContext</div>;
}
